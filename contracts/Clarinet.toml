[project]
name = 'contracts'
description = ''
authors = []
telemetry = false
cache_dir = '.\.cache'
requirements = []
[contracts.stackcred-trait]
path = "contracts/stackcred-trait.clar"
clarity_version = 2
epoch = 2.5

[contracts.stackcred-app]
path = 'contracts/stackcred-app.clar'
clarity_version = 3
epoch = 'latest'
[repl.analysis]
passes = ['check_checker']

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false

[repl.remote_data]
enabled = false
api_url = 'https://api.hiro.so'
